VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Leave these blank if you want to publish a local-only GitHub Pages version without a database.
# Add the Supabase values later when you want shared accounts and sync across devices.

# Store the user's role in Supabase app_metadata.role or user_metadata.role
# Valid values: member, intercessor, pastor, prayer-core